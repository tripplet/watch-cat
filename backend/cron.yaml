cron:
- description: cleanup blocked ips
  url: /cron/unblock
  schedule: every 2 hours

- description: check jobs every 6 hours (as backuk incase tasks fail)
  url: /cron/checkjobs
  schedule: every 2 hours

- description: cleanup logs
  url: /cron/log_cleanup
  schedule: every sunday 03:00
